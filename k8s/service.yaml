apiVersion: v1
kind: Service
metadata:
  name: sentinelnet-service
  namespace: default
  labels:
    app: sentinelnet
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: sentinelnet
